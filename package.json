{
  "name": "community-bot",
  "version": "3.9.20",
  "description": "All-in-One Discord Community Bot — ticket panel (multi-category), auto-responder, anti-spam, AFK, leveling, key-driven VIP role, self-role, temp voice, giveaway, scheduled announcements, embed builder, backup, warn, stats, poll — fully configurable from Discord",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:unit": "node --test \"tests/unit/**/*.test.js\"",
    "lint": "eslint index.js src/",
    "lint:fix": "eslint index.js src/ --fix",
    "format": "prettier --write \"**/*.{js,json,md,yml}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,json,md,yml}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.1"
  }
}
